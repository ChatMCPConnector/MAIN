name: Diagnose classic Unity ULF activation

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  UNITY_IMAGE: unityci/editor:ubuntu-6000.3.17f1-windows-mono-3
  UNITY_SERIAL: F4-9QCB-A9YM-9MHS-CQR2-WKN2

jobs:
  activation-probe:
    name: Probe classic Unity Personal activation
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4

      - name: Prepare diagnostics
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci-diagnostics
          echo "serial=$UNITY_SERIAL" > ci-diagnostics/probe.txt

      - name: Pull UnityCI editor image
        shell: bash
        run: docker pull "$UNITY_IMAGE"

      - name: Activate exactly like GameCI
        id: activate
        continue-on-error: true
        shell: bash
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        run: |
          set +e
          docker run --rm \
            --workdir /github/workspace \
            --volume "$GITHUB_WORKSPACE:/github/workspace" \
            --env UNITY_SERIAL \
            --env UNITY_EMAIL \
            --env UNITY_PASSWORD \
            "$UNITY_IMAGE" \
            bash -lc '
              set +e
              UNITY_BIN="$(command -v unity-editor 2>/dev/null || true)"
              if [[ -z "$UNITY_BIN" && -n "${UNITY_PATH:-}" && -x "$UNITY_PATH/Editor/Unity" ]]; then
                UNITY_BIN="$UNITY_PATH/Editor/Unity"
              fi
              if [[ -z "$UNITY_BIN" && -x /opt/unity/Editor/Unity ]]; then
                UNITY_BIN=/opt/unity/Editor/Unity
              fi
              if [[ -z "$UNITY_BIN" ]]; then
                UNITY_BIN="$(find /opt/unity -type f -path "*/Editor/Unity" -perm -111 2>/dev/null | head -n 1)"
              fi
              echo "unity_bin=${UNITY_BIN:-missing}" >> /github/workspace/ci-diagnostics/probe.txt
              if [[ -z "$UNITY_BIN" ]]; then
                echo 90 > /github/workspace/ci-diagnostics/activation-exit-code.txt
                exit 90
              fi

              "$UNITY_BIN" \
                -batchmode -nographics -quit \
                -serial "$UNITY_SERIAL" \
                -username "$UNITY_EMAIL" \
                -password "$UNITY_PASSWORD" \
                -logFile /github/workspace/ci-diagnostics/activation.log
              activation_exit=$?
              echo "$activation_exit" > /github/workspace/ci-diagnostics/activation-exit-code.txt

              "$UNITY_BIN" \
                -batchmode -nographics -quit -returnlicense \
                -username "$UNITY_EMAIL" \
                -password "$UNITY_PASSWORD" \
                -logFile /github/workspace/ci-diagnostics/return-license.log >/dev/null 2>&1 || true
              exit "$activation_exit"
            '
          exit_code=$?
          echo "exit_code=$exit_code" >> "$GITHUB_OUTPUT"
          exit "$exit_code"

      - name: Create activation report
        if: always()
        shell: bash
        env:
          STEP_OUTCOME: ${{ steps.activate.outcome }}
          ACTIVATION_EXIT_CODE: ${{ steps.activate.outputs.exit_code }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        run: |
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          root = Path('ci-diagnostics')
          root.mkdir(exist_ok=True)
          report = root / 'classic-ulf-activation-summary.txt'
          secrets = [os.environ.get('UNITY_EMAIL', ''), os.environ.get('UNITY_PASSWORD', '')]
          secrets = [value for value in secrets if value]
          pattern = re.compile(
              r'error|exception|fail(?:ed|ure)?|licen[cs]e|activat|serial|'
              r'unauthori[sz]ed|invalid|denied|entitlement|machine|binding|token|'
              r'request|response|status|return code|exit code',
              re.IGNORECASE,
          )

          def redact(text: str) -> str:
              for value in secrets:
                  text = text.replace(value, '[REDACTED_SECRET]')
              text = re.sub(r'(?i)(password\s*[=:]\s*)\S+', r'\1[REDACTED]', text)
              return text

          sections = [
              'Classic Unity ULF activation probe',
              'serial=F4-9QCB-A9YM-9MHS-CQR2-WKN2',
              f"step_outcome={os.environ.get('STEP_OUTCOME', '')}",
              f"action_exit_code={os.environ.get('ACTIVATION_EXIT_CODE', '')}",
          ]
          for small in ['probe.txt', 'activation-exit-code.txt']:
              path = root / small
              if path.exists():
                  sections.extend(['', f'===== {small} =====', redact(path.read_text(errors='replace'))])

          for name in ['activation.log', 'return-license.log']:
              path = root / name
              lines = path.read_text(encoding='utf-8', errors='replace').splitlines() if path.exists() else []
              sections.extend(['', f'===== {name} (exists={path.exists()}, lines={len(lines)}) ====='])
              selected = set(range(max(0, len(lines) - 260), len(lines)))
              for index, line in enumerate(lines):
                  if pattern.search(line):
                      selected.update(range(max(0, index - 5), min(len(lines), index + 6)))
              previous = -2
              for index in sorted(selected):
                  if index > previous + 1:
                      sections.append('...')
                  sections.append(f'{index + 1}: {redact(lines[index])}')
                  previous = index

          report.write_text('\n'.join(sections) + '\n', encoding='utf-8')
          print(report.read_text(encoding='utf-8'))
          PY

      - name: Upload activation diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: NeonRift-Classic-ULF-Activation-Diagnostics
          path: ci-diagnostics/classic-ulf-activation-summary.txt
          if-no-files-found: error
          retention-days: 7

      - name: Fail when activation failed
        if: steps.activate.outcome != 'success'
        shell: bash
        run: exit 1
