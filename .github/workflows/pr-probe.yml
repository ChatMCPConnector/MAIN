name: PR Trigger Probe

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm pull request trigger
        run: echo "Pull request workflows are active."
