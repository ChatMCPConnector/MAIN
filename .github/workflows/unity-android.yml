name: Android prototype

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push validation is temporarily paused during the modular phase-2 update."
