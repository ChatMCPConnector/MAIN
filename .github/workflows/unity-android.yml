name: Android prototype

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  integration-in-progress:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Final coop and release integration in progress"
