diff --git a/src/main/index.ts b/src/main/index.ts
index 5066658..706c1ed 100644
--- a/src/main/index.ts
+++ b/src/main/index.ts
@@ -15,6 +15,16 @@ process.on('unhandledRejection', (reason) => {
   console.error('Unhandled Rejection:', reason)
 })
 
+// Headless-Umgebungen (Codespace/Xvfb): Hardware-Generierung komplett abschalten,
+// sonst startet der GPU-Prozess trotzdem (Software-Compositing-Polling = Dauerlast).
+if (!process.env.DISPLAY || process.env.XVFB_RUN || process.env.ELECTRON_HEADLESS) {
+  app.disableHardwareAcceleration()
+  app.commandLine.appendSwitch('disable-gpu')
+  app.commandLine.appendSwitch('disable-software-rasterizer')
+  app.commandLine.appendSwitch('disable-gpu-compositing')
+  console.log('Headless environment detected: hardware acceleration disabled')
+}
+
 // Workaround for V8 JIT compiler crash on macOS ARM64 (Electron 33 bug)
 // Completely disable JIT compilation to prevent EXC_BAD_ACCESS crashes
 // This trades some performance for stability
diff --git a/src/main/providers/builtin/glm.ts b/src/main/providers/builtin/glm.ts
index 7ceb77a..b7f80cb 100644
--- a/src/main/providers/builtin/glm.ts
+++ b/src/main/providers/builtin/glm.ts
@@ -31,12 +31,16 @@ export const glmConfig: BuiltinProviderConfig = {
     'X-Lang': 'zh',
   },
   enabled: true,
-  description: 'Zhipu Qingyan AI assistant, supports GLM-5.1 flagship model, deep thinking and video generation',
+  description: 'Zhipu Qingyan AI assistant, supports GLM-5.1/GLM-5.3 flagship models, deep thinking and video generation',
   supportedModels: [
     'GLM-5.1',
+    'GLM-5.3',
+    'GLM-5.3-Think',
   ],
   modelMappings: {
     'GLM-5.1': 'glm-5.1',
+    'GLM-5.3': 'glm-5.3',
+    'GLM-5.3-Think': 'glm-5.3-think',
   },
   credentialFields: [
     {
diff --git a/src/main/proxy/adapters/glm.ts b/src/main/proxy/adapters/glm.ts
index f4e8faa..bb3b2a6 100644
--- a/src/main/proxy/adapters/glm.ts
+++ b/src/main/proxy/adapters/glm.ts
@@ -11,6 +11,45 @@ import { PassThrough } from 'stream'
 import { createParser } from 'eventsource-parser'
 import FormData from 'form-data'
 import mime from 'mime-types'
+
+// Merge-Fix: Der Upstream sendet bei init-Events TOKEN-Schnipsel im text-Feld
+// (keine Akkumulation!) und im finish-Event den kompletten Text. Schnipsel
+// werden an den Akkumulat angehaengt; ein finish-Volltext, der mit dem
+// Akkumulat beginnt, ersetzt ihn idempotent. (Blind-Replace erzeugte
+// ueberlappende Muell-Deltas wie '{"ol_callh".)
+function mergePartTexts(existing: any, incoming: any): any {
+  const merged = { ...existing }
+  const incContent = incoming.content
+  if (!Array.isArray(incContent)) return merged
+  let oldText = ''
+  const nonTextOld: any[] = []
+  if (Array.isArray(existing.content)) {
+    for (const item of existing.content) {
+      if (item && item.type === 'text') oldText += String(item.text ?? '')
+      else nonTextOld.push(item)
+    }
+  }
+  const incomingStatus = String(incoming.status ?? '')
+  let newText = oldText
+  for (const item of incContent) {
+    if (!item || item.type !== 'text') continue
+    const fragment = String(item.text ?? '')
+    if (!fragment) continue
+    if (incomingStatus === 'finish' && oldText && fragment.startsWith(oldText)) {
+      newText = fragment
+    } else {
+      newText = newText + fragment
+    }
+  }
+  const content: any[] = []
+  if (newText) content.push({ type: 'text', text: newText })
+  for (const item of nonTextOld) content.push(item)
+  for (const item of incContent) {
+    if (item && item.type !== 'text') content.push(item)
+  }
+  merged.content = content
+  return merged
+}
 import path from 'path'
 import { toolsToSystemPrompt, TOOL_WRAP_HINT, hasToolPromptInjected } from '../utils/tools'
 import { parseToolCallsFromText } from '../utils/toolParser'
@@ -408,6 +447,25 @@ export class GLMAdapter {
       return acc
     }, '')
 
+    // Agent-Loop-Fix: Endet die History auf tool-Ergebnisse (typisch für Agenten-Frameworks
+    // wie opencode, die nach Tool-Ausführung KEINE neue user-message senden), wird das
+    // Modell sonst in einen "Zusammenfassungs"-Chat-Modus gezwungen und hört auf, Tools
+    // zu callen. Ergänze in dem Fall eine explizite Agent-Fortsetzungs-Aufforderung,
+    // damit der Loop weitergeht (nur wenn Tools im Spiel sind).
+    const lastMsg = processedMessages[processedMessages.length - 1]
+    if (
+      toolsPrompt &&
+      lastMsg &&
+      (lastMsg.role === 'user' || (lastMsg as any).tool_call_id) &&
+      processedMessages.some(m => m.role === 'assistant' && (m as any).tool_calls)
+    ) {
+      const lastIsToolResult = (lastMsg as any).tool_call_id !== undefined
+        || (lastMsg.role === 'user' && (lastMsg as any).tool_call_id === undefined && messages[messages.length - 1].role === 'tool')
+      if (lastIsToolResult) {
+        textContent = textContent.trim() + "\n\nIMPORTANT: You are in an agent loop. The tool results above are fresh. Continue the task: either call the next tool (in the [function_calls] block) or, if everything is done, give your final answer. Do NOT summarize the conversation — act."
+      }
+    }
+
     // Inject tools prompt at the VERY END
     if (toolsPrompt) {
       textContent = textContent.trim() + "\n\n" + toolsPrompt
@@ -751,7 +809,7 @@ export class GLMStreamHandler {
               result.parts.forEach((part: any) => {
                 const index = cachedParts.findIndex((p) => p.logic_id === part.logic_id)
                 if (index !== -1) {
-                  cachedParts[index] = part
+                  cachedParts[index] = mergePartTexts(cachedParts[index], part)
                 } else {
                   cachedParts.push(part)
                 }
@@ -961,12 +1019,12 @@ export class GLMStreamHandler {
 
             if (result.status !== 'finish') {
               if (result.parts) {
-                // Accumulate parts (same as handleStream), don't replace
+                // Accumulate parts (merge fix), don't replace
                 // GLM sends incremental parts, each event only contains new content
                 result.parts.forEach((part: any) => {
                   const index = cachedParts.findIndex((p) => p.logic_id === part.logic_id)
                   if (index !== -1) {
-                    cachedParts[index] = part
+                    cachedParts[index] = mergePartTexts(cachedParts[index], part)
                   } else {
                     cachedParts.push(part)
                   }
diff --git a/src/main/proxy/constants/signatures.ts b/src/main/proxy/constants/signatures.ts
index 86aad57..c68b8c4 100644
--- a/src/main/proxy/constants/signatures.ts
+++ b/src/main/proxy/constants/signatures.ts
@@ -156,7 +156,12 @@ export const GENERAL_TOOL_SIGNATURES = [
   'Tool Call Formatting',
   'TOOL USE',
   '## Tool Use',
-  '## Tools',
+  // '## Tools' entfernt: zu grob — opencode (und andere Agent-Frameworks)
+  // nutzen "## Tools" als harmlose Abschnitts-Überschrift OHNE eigene
+  // Tool-Prompt-Injection. Das Skippen der Proxy-Injection nahm dem Modell
+  // die Tool-Definitionen komplett weg ("Ich kann die Tools nicht nutzen").
+  // Wer wirklich selbst injiziert, trägt [function_calls]-Anweisungen o.ä.
+  // (siehe Signaturen oben) mit.
 ]
 
 /**
diff --git a/src/main/proxy/toolCalling/protocols/managedBracket.ts b/src/main/proxy/toolCalling/protocols/managedBracket.ts
index f13b23e..a7077f9 100644
--- a/src/main/proxy/toolCalling/protocols/managedBracket.ts
+++ b/src/main/proxy/toolCalling/protocols/managedBracket.ts
@@ -26,7 +26,9 @@ When calling tools, respond with only this block:
 
 [function_calls]
 [call:exact_tool_name]{"argument":"value"}[/call]
-[/function_calls]`
+[/function_calls]
+
+IMPORTANT: After the closing [/function_calls] tag, always append [] immediately (e.g. [/function_calls][]). The trailing [] is a mandatory terminator that marks the end of the tool call block.`
   },
 
   detectStart(buffer) {
diff --git a/src/main/proxy/toolCalling/providerProfiles.ts b/src/main/proxy/toolCalling/providerProfiles.ts
index d879552..f45d1b8 100644
--- a/src/main/proxy/toolCalling/providerProfiles.ts
+++ b/src/main/proxy/toolCalling/providerProfiles.ts
@@ -22,6 +22,21 @@ const chat2ApiXmlHistoryProfile: Omit<ProviderToolProfile, 'providerId'> = {
   },
 }
 
+// GLM (chatglm.cn): nutzt das Bracket-JSON-Protokoll — arbeitet MIT den
+// JSON-Pretrained-Multipliern des Modells statt gegen sie (XML-Markup
+// wurde vom Modell staendig verunstaltet). History-Format bleibt XML.
+const glmToolHistoryProfile: Omit<ProviderToolProfile, 'providerId'> = {
+  managedSupport: true,
+  supportsNativeTools: false,
+  preferredManagedProtocol: 'managed_bracket',
+  formatAssistantToolCalls(calls) {
+    return managedXmlProtocol.formatAssistantToolCalls(calls)
+  },
+  formatToolResult(result) {
+    return managedXmlProtocol.formatToolResult(result)
+  },
+}
+
 const profiles: Record<string, ProviderToolProfile> = {
   deepseek: {
     providerId: 'deepseek',
@@ -33,7 +48,7 @@ const profiles: Record<string, ProviderToolProfile> = {
   },
   glm: {
     providerId: 'glm',
-    ...chat2ApiXmlHistoryProfile,
+    ...glmToolHistoryProfile,
   },
   qwen: {
     providerId: 'qwen',
diff --git a/src/main/window/manager.ts b/src/main/window/manager.ts
index 09e8719..230515c 100644
--- a/src/main/window/manager.ts
+++ b/src/main/window/manager.ts
@@ -67,6 +67,12 @@ export function createWindow(options: WindowOptions = {}): BrowserWindow {
   })
 
   mainWindow.once('ready-to-show', () => {
+    // Headless (Codespace/Xvfb): Fenster unsichtbar lassen — sonst rendert Chromium
+    // das Dashboard endlos weiter (Dauer-CPU-Last). Proxy läuft im Main-Prozess.
+    if (process.env.ELECTRON_HEADLESS) {
+      console.log('Headless mode: window stays hidden (proxy-only operation)')
+      return
+    }
     mainWindow?.show()
     mainWindow?.focus()
   })
